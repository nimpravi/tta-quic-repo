# Superseded artifacts — retained as evidence, not for use

Every file here is superseded by the canonical record in `../RESULTS.md`
(v2, Sections 7–8 document the mechanisms). Summary of status:

- `headline_K8_run.txt` — the K=8 headline (+4.34 ± 0.36).
  Produced by an undocumented two-phase hybrid created by a
  state-mutating evaluation probe (RESULTS §7.1). NOTE: this file's
  own header claims numerical identity with the repo script; that
  claim is INCORRECT and was written in good faith at archive time.
  The file is preserved unmodified as evidence.
- `mechanism_K3_run.txt`, `mechanism_progress_steps100.json` — clean
  final-eval run, superseded only because the honest frozen config is
  steps=50 (RESULTS §7.5). Still cited as step-sensitivity evidence.
- `ordering_effect_progress.json`, `ordering_generality_run.txt` —
  inputs to the retracted ordering claim (RESULTS §7.3); the claim
  mixed algorithm vintages, not an error in these runs themselves.
- `bnstats_progress_steps100.json`, `collapse_check_q0.5_steps100.json`
  — clean runs at the superseded steps=100 exposure; retained as the
  100-step column evidence behind Table II of the manuscript.

- `step1_single_ordering_run.txt`, `headline_K8_perwindow_run.txt` —
  restored from my saved console output of these runs (numbers
  verbatim; line breaks re-flowed, as noted in each file's header).
  The K=8 per-window envelopes here are what verified the leakage-demo
  reconstruction (RESULTS §8.3).
