# Raw artifacts — canonical runs (pinned environment)

Console logs (`*.txt` / `*_run.txt`) and resumable JSON checkpoints
for every number in `../RESULTS.md` v2 and v2.1 and in the manuscript.
File-to-result mapping is given in RESULTS.md §9–10. The
`determinism_audit_run.txt` documents the within-process
bit-determinism that underpins the ordering-based error-bar design.
