# Pinned loosely; exact versions from the author's tent-env (Python 3.12, CPU).
# Adjust as needed for your environment.
torch>=2.0
numpy>=1.24
scikit-learn>=1.3
cesnet-datazoo~=0.2.0
cesnet-models~=0.4.0